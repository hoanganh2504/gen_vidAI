services:
  food-video-ai:
    image: food-video-ai:demo
    container_name: food-video-ai
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

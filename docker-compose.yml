services:
  food-video-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: food-video-ai
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 8000
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
